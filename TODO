====================================================================================================================================================================================
TLS proxying:
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Incoming connection type = ssl
	tlsPipe1, server = net.Pipe()
	nextClient = tls.Server(tlsPipe1)
	server.Write(headerData)
	go io.Copy(client, server)
	go io.Copy(server, client)
	go nextClient.Handshake()
	go dissectSubTLSConnection()
====================================================================================================================================================================================