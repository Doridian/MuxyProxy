========================================================================================================================
TLS proxying:
------------------------------------------------------------------------------------------------------------------------
Incoming connection type = ssl => server, tlsPipe = net.Pipe() => tls.Server(tlsPipe), oldDataWrite + io.Copy code.
Inject another loop detecting subconnection type.
========================================================================================================================